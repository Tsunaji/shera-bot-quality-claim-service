const { CardFactory, ActionTypes } = require('botbuilder');
const ProductList = require('../resources/productEN.json');
const path = require('path');
const fs = require('fs');

module.exports = {
    
}